// Declaration
let arr = new Array();
let arr = new Array(2); // will it create an array of [2] ?
alert( arr.length ); // length 2

let arr1 = [];

let apple = [ 'Apple', { name: 'John' }, true, function() { alert('hello'); } ];
apple[0] // Apple


apple.length // 3
fruits[123] = "Apple";
alert( fruits.length ); // 124
arr.length = 2; // truncate to 2 elements


apple.pop // remove "Pear" and alert it
apple.push('new', 'set') // append the element to the end of the array

apple.shift() // remove first and alert it
apple.unshift() // add the element to the beginning of the Array
